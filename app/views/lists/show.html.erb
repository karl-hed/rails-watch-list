<h1>Individual List</h1>
<%= @list.name %>
<% @list.movies.each do |movie| %>
  <% bookmark = Bookmark.find_by(list: @list, movie: movie) %>

  <div class="container">
    <%= movie.title %>
    <div>
      <%= image_tag(movie.poster_url, size:400, class:"card-category mt-5", crop: :fill) %>
      <p><%= movie.overview %></p>
    </div>

  </div>



  <div class="container">
    <div class="card-trip">
      <img src="https://raw.githubusercontent.com/lewagon/fullstack-images/master/uikit/greece.jpg" />
      <div class="card-trip-infos">
        <div>
          <h2>Title here</h2>
          <p>Short description here!</p>
        </div>
      </div>
    </div>
  </div>



  <br>
  <div class="container">
    <div class="card-product">
      <%= image_tag(movie.poster_url) %>
      <!-- img src="https://raw.githubusercontent.com/lewagon/fullstack-images/master/uikit/skateboard.jpg" / -->
      <div class="card-product-infos">
        <h2><%= movie.title %></h2>
        <p><%= movie.overview %></p>
      </div>
      <%= link_to 'Delete Bookmark',
        bookmark_path(bookmark),
        data: { turbo_method: :delete, turbo_confirm: 'Are you sure?' },
        class: "btn btn-primary"
      %>
    </div>
  </div>


  <br>
  <div class="container">
    <%= link_to 'Delete Bookmark',
        bookmark_path(bookmark),
        data: { turbo_method: :delete, turbo_confirm: 'Are you sure?' },
        class: "btn btn-primary"
    %>
  </div>

  <!--
  <p>
    <div class="container">
      <%= movie.title %>
      <br>
      <%= image_tag(movie.poster_url, size:200) %>
      <p>
        <%= movie.overview %>
      </p>
      <div class="container">
        <%= link_to 'Delete Bookmark',
            bookmark_path(bookmark),
            data: { turbo_method: :delete, turbo_confirm: 'Are you sure?' },
            class: "btn btn-primary"
        %>
      </div>
    </div>
  </p>
  -->
<% end %>

<%= link_to 'Back', lists_path %>
<div class="container">
  <%= link_to 'Add Bookmark', new_list_bookmark_path(@list), class: "btn btn-primary" %>
</div>
